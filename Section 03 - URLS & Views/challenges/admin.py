from django.contrib import admin
# *NOTES*
# This line imports the `admin` module from `django.contrib`.
# The `admin` module is used to register models with the Django admin site.
# https://docs.djangoproject.com/en/5.0/ref/contrib/admin/

# Register your models here.
# *NOTES*
# This is a placeholder comment suggesting where to register models with the admin site.
# Registering a model with the admin site allows you to manage it through the Django admin interface.
# https://docs.djangoproject.com/en/5.0/ref/contrib/admin/#django.contrib.admin.ModelAdmin

# This is covered later